// Core
import React from 'react';

// Components
import { Flex, Heading } from 'reas';

const AppTitle = () => {
    return (
        <Flex justifyContent="center">
            <Heading
                textTransform="uppercase">WC Orders App</Heading>
        </Flex>
    );
};

export default AppTitle;
